---
title: "Questions"
description: "A question is exactly what it says, a question asked to a dashboard. It can pull data from the underlying datasources in the dashboard and has a chart output as well as a human readable markdown output."
icon: "circle-question"
---

### Question fields

<ParamField path="id" type="uuid">
  The unique identifier for the question. Automatically set if not provided.
</ParamField>

<ParamField path="created_at" type="datetime">
  The timestamp indicating when the question was created. Automatically set if
  not provided.
</ParamField>

<ParamField path="team" type="string">
  The identifier of the team to which the question belongs. Automatically set if
  not provided
</ParamField>

<ParamField path="dashboard" type="string" required>
  The identifier of the dashboard associated with the question.
</ParamField>

<ParamField path="messages" type="array" required>
  The output or result of the question.
  <Expandable title="properties">
    <ParamField path="role" type="string">
      Can be either 'user' or 'assistant'.
    </ParamField>
    <ParamField path="content" type="string">
      The message content in markdown format. Contains the code to run in
      \`\`\`python \`\`\` tags and the code output in \`\`\`json \`\`\` tags
    </ParamField>
  </Expandable>
</ParamField>

<ParamField path="query" type="string" required>
  The query or question posed by the user.
</ParamField>
